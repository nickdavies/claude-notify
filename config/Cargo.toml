[package]
name = "config"
version = "0.1.0"
edition = "2024"

[dependencies]
globset = "0.4"
protocol = { path = "../protocol" }
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
strum = { version = "0.26", features = ["derive"] }
