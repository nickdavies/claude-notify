{
  "name": "opencode-plugin-agent-hub",
  "version": "0.1.0",
  "description": "opencode plugin: routes permission.ask through agent-hub-gateway",
  "type": "module",
  "main": "agent-hub.ts",
  "oc-plugin": ["server"],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "generate:schemas": "cd ../.. && cargo run -p protocol --bin generate_schemas -- provider-plugins/opencode/generated",
    "generate:types": "bun run generate-types.ts",
    "generate": "bun run generate:schemas && bun run generate:types",
    "check": "bun run generate && bun run typecheck"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.3.17"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@types/node": "^25.5.0",
    "json-schema-to-typescript": "^15.0.4"
  }
}
